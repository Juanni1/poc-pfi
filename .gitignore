# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Logs de corridas (las métricas se regeneran en cada ejecución)
metrics.log

# Artefactos pesados de EZKL / zkML (se regeneran; ver CLAUDE.md).
# Nota: model.onnx e input.json SÍ se versionan (son la referencia reproducible).
*.key
pk.key
vk.key
*.srs
kzg.srs
settings.json
calibration.json
witness.json
proof.json
network.compiled

# Artefactos del verifier EVM (Plan 3, regenerables)
*.sol.bin
*.abi
Verifier.sol
abi.json
verifier_addr.txt
calldata*
artifacts/
cache/

# Node / Hardhat (se reinstala con npm install)
node_modules/

# Secretos de testnet (Plan 4): RPC y clave privada de prueba. NUNCA commitear.
.env

# IDE / OS
.idea/
.vscode/
.DS_Store
